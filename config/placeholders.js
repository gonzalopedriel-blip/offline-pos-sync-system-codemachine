const path = require('node:path');

// ========================================
// offline-pos-sync-system Workflow - Placeholders
// ========================================
{
  // Placeholders for agent outputs (referenced in prompts)
  userDir: {
    // Agent output artifacts - referenced as placeholders in subsequent agents
  },
  packageDir: {
    // Shared files would go here if needed
  },
};